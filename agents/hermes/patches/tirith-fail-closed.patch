diff --git a/tools/tirith_security.py b/tools/tirith_security.py
--- a/tools/tirith_security.py
+++ b/tools/tirith_security.py
@@ -749,2 +749,8 @@ def check_command_security(command: str) -> dict:
     if _circuit_open:
-        return {"action": "allow", "findings": [], "summary": "tirith disabled (circuit breaker)"}
+        if cfg["tirith_fail_open"]:
+            return {"action": "allow", "findings": [], "summary": "tirith disabled (circuit breaker)"}
+        return {
+            "action": "block",
+            "findings": [],
+            "summary": "tirith unavailable, circuit breaker open (fail-closed)",
+        }
